using UnityEngine;

public static class Define
{
    public enum ETileType
    {
        None,
        Player,
        Enemy,
    }

    public enum EInputType
    {
        Up,
        Down,
        Left,
        Right,
    }
}
